{
  "name": "bpnz-membership",
  "version": "0.1.0",
  "private": true,
  "description": "BPNZ membership platform and governance reminders Worker (Cloudflare)",
  "type": "module",
  "scripts": {
    "compile:reminders": "node scripts/compile-reminders.mjs",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "npm run compile:reminders && wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0",
    "wrangler": "^4.0.0",
    "yaml": "^2.5.0"
  }
}
